import { FilterAssetsPipePipe } from './filter-assets-pipe.pipe';

describe('FilterAssetsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssetsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
